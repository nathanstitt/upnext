
// Configuration settings for OpenWeather library

// These parameters set the data point count stored in program memory (not the
// datapoint count sent by the server). So they determine the memory used during
// collection of the data points.

